[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app_fullstack:app --host 0.0.0.0 --port ${PORT:-3003}"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
